# T02 Expansion and I04 Completion Plan

> Execute on the shared `main` checkout only. Hold `/tmp/friendly-bot-main-mutation-ryanthe.lock` around each commit/reconcile/push sequence. Never read or mutate the historical `bot2` runtime.

**Spec:** `docs/superpowers/specs/2026-09-14-t02-telegram-native-output-i04-completion-design.md`

## 1. Closed config and callback boundary

- [ ] Add red unit tests for stable, service-context, and match-context callback round trips;
  malformed/version/byte-limit rejection; and typed inbound normalization.
- [ ] Replace the generic button payload model with the canonical closed service-key payload.
- [ ] Implement the callback codec and modify polling/dispatcher contracts so I04 receives the
  typed callback rather than a raw callback string.
- [ ] Run targeted domain/Telegram tests, Ruff, formatting, and mypy; commit the boundary.

## 2. T02 native output and asset resolver

- [ ] Add red tests for text/photo inline keyboards, multipart photo request construction,
  activity dispatch, legacy text rows, strict payload rejection, and no replay after an
  uncertain send.
- [ ] Add closed Telegram output DTOs, Bot API serialization, outbox mapping, and immediate
  activity operation while preserving claim/start/finalize behavior.
- [ ] Add the catalog-backed asset resolver; commit `assets/zone_x_poster_2026.png` and its
  SHA-256/MIME catalog entry only after resolver tests prove the exact asset validates.
- [ ] Run T02 unit and PostgreSQL integration suites and static checks; commit T02 expansion.

## 3. F01 match API completion

- [ ] Add red repository tests for request creation/ownership, meeting preference, assignment
  lookup, responder contact, release/exclude, and concurrent capacity fencing.
- [ ] Add frozen DTOs and guarded repository methods using the existing UoW/service/request
  locks; do not add tables or migrations.
- [ ] Run F01 repository/schema PostgreSQL checks and static checks; commit the bounded repair.

## 4. I04 executors and action composition

- [ ] Add red tests for the presentation buffer, orphan-button failure, all 24 explicit
  executors, strict service/match callback revalidation, and match notification/contact flow.
- [ ] Implement `actions/executors.py`, extend `ActionContext` and runner flushing behavior,
  and build the complete closed registry. No executor calls OpenRouter.
- [ ] Run action focused tests and static checks; commit.

## 5. Application, seed, and acceptance

- [ ] Add red tests for direct button/command dispatch, routing terminal copy, sanitised
  diagnostics, canonical Zone X YAML/JSON equivalence, seed idempotency, and all timestamp
  journeys.
- [ ] Implement application composition, canonical Zone X JSON seed, module entry point, and
  local `ryanthe` runtime guide with an unused, collision-checked port.
- [ ] Run all unit, integration, e2e, PostgreSQL migration/canary, and static checks. Make no
  live Telegram send or provider request without a separately validated local configuration.
- [ ] Request independent review, update truthful T02/I04 execution evidence, and only then
  report G3-ready results; do not claim coordinator gate passage.
