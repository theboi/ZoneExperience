# R03 Planning Status

| Field | Value |
| --- | --- |
| Worker | R03 — constrained routing, persona, and matching |
| Phase | Planning complete / execution blocked |
| Inspected integration authority | `origin/main` at planning commit `8b37b950f057db02802a5c2333de0357d241036c` |
| Hard execution prerequisites | Coordinator-passed `PG` and F01 `G1` evidence reachable from `origin/main` |
| Owned planning artifacts | R03 dated design, R03 dated plan, this status, and R03 planning manifest |
| Product-authority impact | No product-specification edit required |

## Evidence-backed findings

- The product specification and approved design already settle all user-visible R03 behavior: configured-key routing, native reply ambiguity copy, no-match copy, durable personas, normal/rematch matching, and safety no-fallback handling.
- Architecture assigns OpenRouter privacy enforcement, routing, persona, and matching to R03, explicitly requires ZDR/denied collection, and excludes structured Telegram IDs and DOBs.
- F01 planning commit `a920d2d` publishes the required domain and UoW/repository ownership. Its correction removes only a source-coded database credential default. R03's design records the exact direct repository operations it must verify at G1; it does not introduce a persistence fork.
- This Pass 1 commit creates documents only. No application package, model call, migration, test, configuration, or product code has been created.
- Coordinator review correction: prompt DTO privacy evidence uses `extra="forbid"` and sentinel-value negative tests rather than rejecting ordinary user-authored words; the plan ends with an execution acceptance/review/handoff task.

## Downstream planned contracts

- I04 consumes `ConstrainedRouter`, `PersonaMaintenanceService`, and `MatchingService`; I04 owns action executors, Telegram sends, fixed fallback prose, notifications, and runtime composition.
- R03's normal service returns an assignment or `None` for `human_match.not_found`; safety returns an assignment or `None` for `safety_match.not_found`. I04 maps those settled action events to direct child flows.
- The R03 gateway receives prompt-safe text and candidate aliases only. I04 must not add Telegram identifiers, DOBs, raw profile records, or free-form model responses to it.
