# R03 Implementation and Operational Privacy Status

| Field | Value |
| --- | --- |
| Worker | R03 — constrained routing, persona, and matching |
| Phase | Implementation complete; operational privacy blocked; G2 not passed |
| Observability-attestation repair commit | `ab4d46c` |
| Assistant-content source-privacy repair commit | `2f89aba` |
| Envelope source-privacy repair commit | `31b2037` |
| Source privacy re-review | Envelope repair committed; re-review pending |
| F01 G1 evidence inspected | `987f8a2a90090b55285698283297bb99200774a3` |
| Coordinator status inspected | `ca2b2c00db23126dad10cdc82fb271d5375db259` |
| Account-level OpenRouter receipt | Not available from this machine; no logged-in browser session is present |

## Fresh implementation evidence

- After safely rebasing the envelope repair onto T02's two Telegram commits, `uv run pytest tests/unit/routing tests/unit/persona tests/unit/matching tests/integration/routing tests/integration/matching -q && uv run ruff check . && uv run ruff format --check . && uv run mypy src/friendly_bot` exited 0: 53 passed, 1 skipped in 1.05s; Ruff reported “All checks passed!”; 53 files were already formatted; and mypy found no issues in 27 source files.
- The permanent gateway tests prove all R03 prompt DTOs, including nested routing and matching candidates, reject `telegramUserId`, `telegram_chat_id`, and `dob`; actual selection, persona, and matching transport payloads retain ordinary authored words such as “telegram” and “dob” while omitting the identifier sentinels.
- The gateway retries transport and non-2xx failures exactly `ROUTING_MAX_ATTEMPTS` times, then raises the typed closed transport error with no raw request or response attached.
- Malformed successful provider content and malformed response envelopes both raise the typed closed protocol error without retaining raw response data through `__cause__`, `__context__`, message text, or exception arguments; the envelope case is exercised through selection, persona, and matching public operations.
- Source-repair reachability was checked after publication: `git fetch origin main --prune`, `git merge-base --is-ancestor 31b2037722b433c49d06ad901b1bfba5976a5182 origin/main`, and `git rev-parse 31b2037722b433c49d06ad901b1bfba5976a5182 origin/main` all exited 0; the final two SHA values matched exactly.

## Delivered boundary

- OpenRouter requests accept only whitelisted/reserved keys, request ZDR and denied collection, and use prompt-safe DTOs. They do not opt into `logprobs`, debug, trace, or metadata logging fields. `logprobs` is not an OpenRouter prompt-logging control.
- `OpenRouterGateway` fails closed by default. It requires the exact non-secret environment value `FRIENDLY_BOT_OPENROUTER_INPUT_OUTPUT_LOGGING_ATTESTATION=disabled-globally-or-friendly-bot-key-excluded`; this is only an operator attestation, not evidence of the OpenRouter account state.
- Routing selects distinct configured keys only; persona cursor advancement occurs only after a valid nonempty summary. Normal matching remains exact server attendance. Safety matching calls only F01's safety query, which admits exact leader/staff attendance or always-available responders, with no normal fallback. Reservations/rematches remain F01-UoW guarded and durable.

## Operational privacy blocker and gate posture

- User-facing and product scope are unchanged.
- The envelope source-privacy repair is pending re-review; it does not change the separate account-level receipt blocker.
- R03 operation remains blocked pending a redacted account-level receipt showing that OpenRouter Input & Output Logging is disabled globally or that the dedicated Friendly Bot API key is explicitly excluded. This machine cannot produce that receipt; the environment attestation must never be treated as proof.
- G2 is not passed, implied, or approved. It requires the account-level privacy receipt, T02 evidence, and coordinator verification.
- The execution manifest is an implementation handoff record only. It does not authorize live OpenRouter or Telegram calls.
