# T02 Execution Status

| Field | Value |
| --- | --- |
| Worker | T02 — Telegram transport and service delivery |
| Phase | Reopened contract repaired; source and test evidence ready for coordinator review |
| F01 prerequisite inspected | `d7db17a5061e0e351a6fc1996f56105c194acaf0` on `origin/main` |
| T02 delivery commits | `35720bed`, `0f203284`, `0fa86ee3` |
| Review repair prerequisite | `d7db17a` unifies outbound delivery lock order |
| Product-authority impact | None; implementation and evidence only |

## Delivered contract

- Public composition imports are present for `TelegramGateway`, `TelegramPoller`, `OutboundDeliveryWorker`, `OnboardingService`, `OperationalAccountService`, `ServiceAttendanceService`, `ServiceDeliveryScheduler`, and `ServiceLifecycleService`.
- Latecomer confirmation now persists locked attendance with `attendee_kind="latecomer"`; timestamp audiences therefore see the same durable attendance record.
- Scheduler timestamp preparation opens a real, idempotent F01 automatic root and proves it preserves an independently current onboarding branch.
- Durable outbound delivery claims, starts, and recovery use one lock order: delivery row before the singleton outbound-pause row. The two-Unit-of-Work regression proves an expired-claim recovery and stale start complete without a lock cycle; the stale worker loses its claim.
- The deliberate operational-login split remains an approved F01 contract: the sender can remain NBNC while the matched profile owner supplies the operational role. It is not a T02 identity repair target.

## Fresh verification and review

- A loopback-only, auto-removed PostgreSQL 16 container ran `uv run pytest tests/unit/telegram tests/integration/telegram tests/unit/onboarding tests/integration/onboarding tests/unit/services tests/integration/services -q`: **131 passed**, with 3 existing SQLAlchemy `Row.tuple()` deprecation warnings.
- `uv run ruff check .`, `uv run ruff format --check .`, and `uv run mypy src/friendly_bot` all passed; format checked 89 files and mypy checked 39 source files.
- A focused independent rereview of `d7db17a` found no Critical or Important T02 code blocker. It confirmed latecomer enrollment, automatic-root branch preservation, scheduler preparation, outbox fencing, and uniform delivery/pause locking. The reviewer noted only a future test-hardening opportunity to replace short task scheduling sleeps with explicit barriers.

## Historical I04 integration blocker — resolved in this execution

- I04 Task 3 proved that the durable T02 worker maps only `kind="message"` with exactly a text payload. Buttons, photos, and activity records are deterministically rejected as `telegram_message_unsupported`; the direct API client likewise exposes only `sendMessage`.
- This conflicts with the approved product experience and canonical Zone X actions, which require inline buttons, photos, and Telegram activity indicators. No callback-payload encoding or sanctioned asset-resolution boundary exists in the current implementation.
- The prior T02 tests and focused review did not exercise this I04 composition contract. Their passing evidence remains valid for text, polling, onboarding, attendance, scheduling, and delivery fencing, but it is not completion evidence for the required Telegram-native output surface.

## Completion update — 2026-09-14

- The typed callback codec now accepts only stable, service-context, or match-request-context data; inbound normalization gives I04 a typed callback rather than raw callback text.
- Durable outbox rows now carry exact typed text, inline-button, and catalog-photo payloads. The Telegram client implements `sendMessage`, `sendPhoto`, and transient `sendChatAction`; the outbox resolver rejects malformed payloads before any provider call.
- The local catalog verifies `zone_x_poster_2026` by path confinement, MIME signature, and SHA-256 only at the safe outbox claim boundary.
- The scheduler port now lets I04 enqueue every output from a timestamp root in the same claimed Unit of Work rather than flattening the root to one text row. A PostgreSQL regression proves the exact interaction-end timestamp is delivered once before its own lifecycle closure; ordinary post-end timestamps remain rejected.
- The full local source suite passed: **435 passed, 1 skipped** across unit, end-to-end, and PostgreSQL integration groups. Ruff, formatting, and mypy also passed with no deprecation warnings.
- The only non-source canary limitation is local tooling: the `ryanthe` profile uses UID 501, `friendly-bot-ryanthe`, `.runtime/ryanthe/`, and loopback port 5833 (confirmed free), but this machine has no Docker Compose v2 plugin, so the guard stopped before any Compose action. No live Telegram or OpenRouter request was made.

## Handoff posture

T02's bounded repair is complete and ready for coordinator review. This records source and test evidence only; it does not declare the coordinator's combined G2 or G3 gate passed.
