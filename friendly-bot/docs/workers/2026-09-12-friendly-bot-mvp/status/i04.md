# I04 Status

| Field | Value |
| --- | --- |
| Worker | I04 — runtime composition and Zone X acceptance |
| Phase | Execution authorized; Task 1 has not yet started |
| Inspected integration authority | `origin/main` at `d7db17a5061e0e351a6fc1996f56105c194acaf0` before this readiness evidence update |
| Hard execution prerequisites | Passed `PG`, implemented F01 `G1`, completed reviewed T02 evidence, and coordinator-authorized R03 G2 evidence |
| Owned planning artifacts | I04 dated design, I04 dated plan, this status, and I04 planning manifest |
| Product-authority impact | No product-specification edit required |

## Execution-readiness reinspection — 2026-09-14

- `origin/main` was fetched and inspected at exactly `d7db17a5061e0e351a6fc1996f56105c194acaf0`; local `main` matched that commit before this evidence update.
- `PLANNING_GATE.md` remains passed and F01's execution manifest supplies G1, including the latest delivery-before-pause lock-order repair at `d7db17a`.
- T02's execution manifest records 131 focused real-PostgreSQL checks, static verification, completed handoff imports, and a focused rereview with no Critical or Important code blocker.
- R03's execution manifest records a fresh 92-test real-PostgreSQL verification. The account holder/coordinator explicitly directed this run to trust their OpenRouter privacy-setting attestation. This is a coordinator G2 authorization for this run, not a redacted receipt or independent proof.
- The non-secret local `OpenRouterGateway.from_environment()` construction check is not currently accepted. No provider request was made, and I04 source work will not use the key until the fail-closed local configuration accepts it.
- The account holder explicitly directed this run not to use `bot2`. No bot2/u504 runtime resource will be read, changed, or started. A local `ryanthe` runtime profile needs a later explicit namespace and port decision before the I04 canary; source-composition tasks do not require that mutation.
- I04 Tasks 1–7 have not begun. This status update authorizes Task 1 only; it does not claim G3 or a local runtime canary.

## Evidence-backed findings

- The product specification already owns the I04-visible behavior: Telegram-native fixed copy/buttons/photos, direct-child action events, exact default-error text, routing ambiguity/no-match copy, checkpoint/reuse behavior, Zone X before/during/after journeys, matching/safety, timestamps/audiences/expiry, restart safety, diagnostics, and local-only operation.
- The architecture masterplan assigns action registry/terminal-event composition, the non-configurable unhandled-error sender, application composition, CLI, Zone X seed, and end-to-end acceptance to I04. It requires `python -m friendly_bot`, local PostgreSQL, durable T02 work, and an OpenRouter privacy boundary maintained by R03.
- F01 reconciliation commit `9bc9e84c6f553d10c1173c786bcb48b4a8513e86` confirms frozen public DTOs/repositories, makes `users.role` authoritative, and registers `SendServiceChoiceButtonsAction` with fixed service-choice rendering data. T02 reconciliation commit `23350575bfaff92de934d2b47cdb89c498c0ad0d` confirms its exact F01 consumption and preserves the I04 composition boundary; R03 reconciliation commit `db386457aaa72a413ba1cd1afe69ef6e6b2a2ce3` confirms matching joins the authoritative role and returns `RoutingDecision` values in `RoutingResult.selected_keys`. The dated I04 design and plan consume those contracts; they add precise modules, typed composition seams, test cases, runtime commands, and G3 handoff evidence; they create no product policy, live runtime, package code, migration, seed JSON, test, or local database state in Pass 1.
- `friendly-bot/docs/examples/zone-x-service-example.md` is the canonical human-readable service document. The planned `friendly-bot/seeds/zone-x.json` is real development data, semantic JSON equivalence is mandatory, and this work does not approve Zone X venue, copy, contact URL, schedule, or names for production.

## Reconciled cross-plan contracts

- `users.role` is the single role source. F01 has removed `operational_profiles.role`; T02/R03 fixtures and matching queries join profile data to exact `users.role`. I04 creates no temporary duplicate-role adapter.
- F01 registers and validates the canonical `SendServiceChoiceButtonsAction`. It renders only local `resolved_service_options` records from a preceding typed attendance/switch action; I04 owns the concrete executor.
- R03 returns selected routed candidates as ordered `RoutingDecision` values in `RoutingResult.selected_keys`. `DiscussionFlow.next_flow_mode` remains the sole configuration/reuse authority.
- T02 retains the documented poller dispatcher seam and exports (`TelegramPoller`, `TelegramGateway`, `OutboundDeliveryWorker`, onboarding/account, attendance/lifecycle, scheduler, preflight, runtime lock). I04 consumes the composed boundary and never recreates its persistence/outbox/polling semantics.
- At execution, I04 verifies these implemented exports and execution manifests on the same remote lineage. A missing implemented prerequisite follows the frozen blueprint's focused repair rule; I04 does not use an adapter, ORM bypass, or alternate behavior.

## Downstream planned contracts

- I04 provides `ActionContext`, `ActionExecutorRegistry`, `ActionRunner`, `FriendlyBotApplication`, `build_application`, and the `python -m friendly_bot` entry point. An executor is keyed only by a concrete registered F01 action class, and emits no more than one terminal `ActionEvent`.
- An emitted event examines only immediate `next_flows`. A direct `error` child runs when present; otherwise code locally renders exactly `Sorry, an error occurred. Error log: {telegram_user_id}.`. The sender is not a flow/configuration/root/seed action and does not send that ID to OpenRouter.
- I04 publishes Zone X from `seeds/zone-x.json`, validates semantic equality with the canonical YAML service document, and supplies end-to-end/restart/local-runtime evidence to the coordinator. It records G3-ready evidence only; coordinator/Ryan owns the G3 pass decision.
