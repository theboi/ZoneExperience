# I04 Planning Status

| Field | Value |
| --- | --- |
| Worker | I04 — runtime composition and Zone X acceptance |
| Phase | Planning complete / execution blocked |
| Inspected integration authority | `origin/main` at `9b5b4e5c7fef5b3a215572cc3f74174590dc5a2d` |
| Hard execution prerequisites | Coordinator-passed `PG`, implemented F01 `G1`, and implemented T02/R03 `G2` evidence, all reachable from `origin/main` |
| Owned planning artifacts | I04 dated design, I04 dated plan, this status, and I04 planning manifest |
| Product-authority impact | No product-specification edit required |

## Evidence-backed findings

- The product specification already owns the I04-visible behavior: Telegram-native fixed copy/buttons/photos, direct-child action events, exact default-error text, routing ambiguity/no-match copy, checkpoint/reuse behavior, Zone X before/during/after journeys, matching/safety, timestamps/audiences/expiry, restart safety, diagnostics, and local-only operation.
- The architecture masterplan assigns action registry/terminal-event composition, the non-configurable unhandled-error sender, application composition, CLI, Zone X seed, and end-to-end acceptance to I04. It requires `python -m friendly_bot`, local PostgreSQL, durable T02 work, and an OpenRouter privacy boundary maintained by R03.
- F01 reconciliation commit `9b5b4e5c7fef5b3a215572cc3f74174590dc5a2d` confirms frozen public DTOs/repositories and makes `users.role` authoritative. T02 reconciliation commit `23350575bfaff92de934d2b47cdb89c498c0ad0d` confirms its exact F01 consumption and preserves the I04 composition boundary; R03 reconciliation commit `50ab700c2256c9c046b481389f6908b9ea3550c4` confirms matching joins the authoritative role. The dated I04 design and plan consume those contracts; they add precise modules, typed composition seams, test cases, runtime commands, and G3 handoff evidence; they create no product policy, live runtime, package code, migration, seed JSON, test, or local database state in Pass 1.
- `friendly-bot/docs/examples/zone-x-service-example.md` is the canonical human-readable service document. The planned `friendly-bot/seeds/zone-x.json` is real development data, semantic JSON equivalence is mandatory, and this work does not approve Zone X venue, copy, contact URL, schedule, or names for production.

## Required cross-plan reconciliation before I04 execution

- Coordinator normalization is binding: `users.role` is the single role source. F01 must remove any duplicated `operational_profiles.role` schema/DTO/query contract; T02/R03 fixtures and matching queries must join profile data to exact `users.role`. I04 creates no temporary duplicate-role adapter.
- The canonical Zone X graph contains `send_service_choice_buttons`; F01 G1 must expose it in the registered `DiscussionAction` union and publication validation. I04 owns its executor after that exact type exists.
- R03 G2 must expose selected routed candidates as `RoutingDecision` values or selected keys in `RoutingResult`; the former `FlowSelection` name is not usable because `DiscussionFlow.next_flow_mode` remains the sole flow-selection/reuse authority.
- T02 G2 must retain the documented poller dispatcher seam and exports (`TelegramPoller`, `TelegramGateway`, `OutboundDeliveryWorker`, onboarding/account, attendance/lifecycle, scheduler, preflight, runtime lock). T02's `23350575bfaff92de934d2b47cdb89c498c0ad0d` contract confirms the composed boundary; I04's dispatcher consumes that seam and never recreates its persistence/outbox/polling semantics.
- At execution, I04 verifies these implemented exports and execution manifests on the same remote lineage. A missing requirement is a focused upstream prerequisite repair under the frozen blueprint; I04 does not proceed with an adapter, ORM bypass, or alternate behavior.

## Downstream planned contracts

- I04 provides `ActionContext`, `ActionExecutorRegistry`, `ActionRunner`, `FriendlyBotApplication`, `build_application`, and the `python -m friendly_bot` entry point. An executor is keyed only by a concrete registered F01 action class, and emits no more than one terminal `ActionEvent`.
- An emitted event examines only immediate `next_flows`. A direct `error` child runs when present; otherwise code locally renders exactly `Sorry, an error occurred. Error log: {telegram_user_id}.`. The sender is not a flow/configuration/root/seed action and does not send that ID to OpenRouter.
- I04 publishes Zone X from `seeds/zone-x.json`, validates semantic equality with the canonical YAML service document, and supplies end-to-end/restart/local-runtime evidence to the coordinator. It records G3-ready evidence only; coordinator/Ryan owns the G3 pass decision.
