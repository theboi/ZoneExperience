# F01 Execution Status

| Field | Value |
| --- | --- |
| Worker | F01 — foundation, flow engine, and persistence |
| Phase | Delivery-contract implementation published at `5b28c2e`; automatic-root and nonblocking-claim repair published at `a98e63a` |
| Inspected integration authority | `origin/main` at `687a9005be7146968d07cf010beefc0c01b17cbd` |
| Reconciled planned contracts | `origin/main` at `968f53f` |
| Hard execution prerequisite | Coordinator-passed `PG` receipt reachable from `origin/main` |
| Owned planning artifacts | F01 dated design, F01 dated plan, this status, and F01 planning manifest |
| Product-authority impact | No product-specification edit required |

## Evidence-backed findings

- The approved product specification already defines the F01-relevant user-visible behavior: recursive flows, immutable versions, open selections, checkpoint returns, durable records, idempotency, and local PostgreSQL.
- The architecture masterplan assigns Pydantic flow definitions, SQLAlchemy persistence, migrations, per-user serialization, and JSONB immutable versions to the F01 boundary.
- The dated F01 design and plan translate those settled requirements into module names, typed interfaces, test examples, commands, PostgreSQL constraints, and a single Alembic sequence. They do not alter approved product behavior; therefore the product masterplan is current for this planning substep.
- The planning correction removes the source-coded database URL credential: `FRIENDLY_BOT_DATABASE_URL` is required, local credentials remain in ignored `.runtime/u504/postgres.env`, and the account-runtime command uses `uv run python` because the machine does not provide a bare `python` executable.
- The reconciliation makes `users.role` the only persisted role source, removes `operational_profiles.role`, and requires matching/audience queries to join profile to user. It also names the complete user, profile, login, service, attendance, poll-state, timestamp, conversation, persona, match, selection, delivery, diagnostic, and flow-version repository operations consumed by T02/R03/I04.
- The three plan test bodies that previously used ellipses now construct and assert concrete transition, attendance-switch, and rollback cases.
- The canonical Zone X review added the missing declarative `send_service_choice_buttons` action discriminator. Its stable button ID and fixed runtime choice-source metadata validate configuration without embedding service records or executor behavior.
- Execution remains blocked. A worker planning commit cannot pass `PG`, and no product code, migration, Compose file, lockfile, or test was created in Pass 1.

## Downstream planned contracts

- T02, R03, and I04 import `friendly_bot.domain` flow DTOs and `friendly_bot.persistence` repository/UoW protocols after F01 execution reaches G1; they do not create migrations or local schema forks.
- I04 owns registered action executors and the hardcoded unhandled-error sender. F01 validates action configuration and direct-child event completeness only.
- A later F01 execution worker must verify a passed coordinator receipt and current remote lineage before beginning Task 1 of the dated plan.

## 2026-09-13 durable delivery-contract repair

- Root-cause ruling: the original F01 delivery record exposed only identity/status, claims had no opaque ownership or recovery lease, retries were scheduled from `created_at`, and `start_attempt` discarded its correlation ID. T02 could not safely send, retry a parsed 429 at its exact time, or reject stale workers without an ORM/persistence bypass.
- F01 repair: additive Alembic `0002_delivery_contract`; durable eligible-at timing; opaque unstarted claim token/expiry; confirmed Telegram message ID; attempt correlation ID; provider-neutral recursively immutable `claim.message`; token-fenced renew/start; exact retry-at and safe result persistence.
- Scope ruling: no product or architecture authority change. The implementation repair needs only F01-owned code/artifacts; review reconciliation updates T02's two dated F01 delivery-contract references without changing T02 ownership, status, or scope. The issue is a missing F01 implementation contract, not a changed user-visible behavior.
- Evidence: three fresh PostgreSQL delivery tests first failed for the absent message/schedule/lease contract and then passed; the `0001`-to-head reversible migration test first failed and then passed; after reconciling the two upstream checkpoint-lineage commits, the implementation commit `5b28c2e` was pushed to `origin/main`.

## 2026-09-13 delivery-contract review repair

- Review finding: the dated T02 contract still showed tokenless `start_attempt` and an uncommitted claim/send sequence. F01 now reconciles only those references to the published F01 contract: committed claim, immutable `claim.message` mapping at the T02 boundary, committed token-fenced start, final safe finish, and no sending/uncertain auto-replay or persistence shim.
- New persistence evidence: `test_recovered_claim_rejects_stale_token_renewal` was run red under a temporary immediately reverted removal of renewal-token validation, then green. The full F01 domain/persistence suite passed with `146 passed, 1 skipped`.
- Authority ruling: product and architecture masterplans remain current because this only completes the approved durable-delivery implementation contract.

## 2026-09-14 runtime-guard verification-test repair

- Root cause: `test_runtime_guard_verify_only_checks_compose_configuration` mocked the Compose subprocess but still executed the real bot2/UID-504 checkout fence. On a non-bot2 development account it failed before exercising the verify action, so its claimed isolated verification behavior was host-dependent.
- Repair: test-only commit `58e1ddfd3b03f94d8db4117374cc07e10119c58d` binds the loaded guard module to the current test checkout and UID before invoking its real namespace validation, while retaining the production guard's fixed bot2/UID-504 boundary unchanged.
- Fresh evidence: `uv run pytest tests/integration/persistence/test_migrations.py -q` reported 7 passed and 8 expected PostgreSQL-dependent skips; Ruff check and format check for the changed test file both exited 0. The repair is reachable from `origin/main`.
- Scope ruling: this fixes deterministic F01 verification only. It does not authorize a runtime for this account, change G1/G2 ownership, or alter T02/R03/I04 behavior.

## 2026-09-14 automatic-root and delivery-claim concurrency repair

- Root cause: I04's required automatic system/service/timestamp roots had no F01 repository operation that could create a root without bypassing persistence or mutating an unrelated branch. Separately, `claim_next_safe` locked the global outbound-pause row before checking a `SKIP LOCKED` delivery, so the established concurrent-claim regression self-blocked instead of returning no work.
- Repair: `OpenSelectionRepository.open_root` now requires the caller's user lock, verifies the published version's exact root key and service scope, fences an active service, inserts idempotently, and never alters another current selection. Delivery claiming selects a due delivery under `SKIP LOCKED` before it locks and reads the global pause row. The schema table inventory now includes the pre-existing `telegram_outbound_pauses` migration/model table.
- Evidence: the root test failed with `AttributeError` before the new method and then passed against a loopback-only disposable PostgreSQL container. The delivery-concurrency test previously stalled at the second claim; it passes after the lock-order repair. Fresh full F01 verification on the same current-account disposable pattern reported `157 passed, 1 skipped`; Ruff, format, and mypy passed. Commit `a98e63a7d36e32c84466ea1c827b1cba1da2c0ff` is reachable from `origin/main`.
- Scope ruling: no product, architecture, role, or privacy rule changes. This completes existing F01 persistence semantics required by the approved I04 automatic-root and T02 outbox contracts.
