# SDD ledger — plan: friendly-bot/docs/superpowers/plans/2026-09-12-friendly-bot-mvp/2026-09-12-f01-foundation-flow-persistence.md

## Preflight contract scan

| Task | Produces / consumes | Internal consistency | Ruling |
| --- | --- | --- | --- |
| 1 | Package tooling and required database settings | Tests, implementation shape, and files agree | Clean |
| 2 | Trigger/action unions consume Zone X discriminators | F01 now includes all 24 canonical action types | Clean |
| 3 | Recursive publication consumes Task 2 unions | Validation cases match approved design | Clean |
| 4 | Selection engine consumes Task 3 flow model | Mode/checkpoint rules and concrete tests agree | Clean |
| 5 | ORM schema consumes domain enums and creates repository backing | Sole `users.role` and complete constraints agree with ARCH-014 | Clean |
| 6 | Repositories/UoW consume Task 5 ORM and Task 4 transitions | Exact T02/R03/I04 protocols are enumerated | Clean |
| 7 | Alembic/Compose consume Task 1 settings and Task 5 metadata | UID 504, project namespace, and port 5832 are consistent | Clean |
| 8 | Acceptance consumes Tasks 1–7 | G1 evidence and manifest scope agree | Clean |
| 1 → 7 | Settings and runtime guard feed Compose/migration | Required environment-only secret path is consistent | Clean |
| 2 → 3 → 4 | Typed config feeds publication and state transition | No executor behavior leaks into F01 | Clean |
| 4/5 → 6 | Pure transitions plus ORM back repositories | DTO boundary prevents ORM leakage | Clean |
| 6/7 → 8 | Repositories and runtime feed G1 | Focused suites precede one final F01 suite | Clean |

No preflight ruling changed the approved specification.

## Execution ledger

- Task 1 — complete and independently approved after review round 1. Commits: `7a5c3a444781aa261e8684633a8ca8657133bf2b`, `77ac5db0c5ee19fce547c2a031f2a3290d137656`. Verification: 3 unit tests, Ruff check/format, strict mypy, lock check, and direct installed-package import passed. Published to `origin/main` at `77ac5db0c5ee19fce547c2a031f2a3290d137656`.
- Task 2 — complete and independently approved after review round 1. Commits: `ba9e6ed60b294466c9a8c9999359302ca4202153`, `98a5c264ace85a66e791bd1fc9a0a3e28a11036b`. Verification: 46 focused tests plus Task 1 tests, Ruff check/format, strict mypy, lock check, canonical Zone X parse, and strict nested JSON serialization passed. Published to `origin/main` at `98a5c264ace85a66e791bd1fc9a0a3e28a11036b`.
- Task 3 — complete and independently approved after review round 1. Commits: `9eefcd5084612fd7ec34c5991ecb22aa7a5cd449`, `e65929d`, `1ee832b`. Verification: 73 tests, Ruff check/format, strict mypy, and lock check passed. Closed-schema normalization, immutable canonical publication storage, exact event coverage, normalized draft errors, and optional-only templates were review-hardened. Published to `origin/main` at `1ee832b`.
- Task 4 — complete and independently approved after review round 1. Commits: `12ccd643810a66bffd7a03393f5dd3f40793a24a`, `5690a4dc1370eb187b3226eef401fcde283187f6`. Verification: 18 focused state tests, 91 full tests, Ruff check/format, strict mypy, and lock check passed. Parent/child and branch/checkpoint validation, immutable lineage, explicit per-update deduplication, and required checkpoint return-action resolution were review-hardened. Published to `origin/main` at `5690a4dc1370eb187b3226eef401fcde283187f6`.

Ruling: Task 5's illustrative `SqlAlchemyAttendanceRepository.start_or_switch` test is deferred to Task 6, which exclusively owns repositories. Task 5 will prove the same durable active-attendance invariant directly through ORM/PostgreSQL constraints. This avoids an early repository implementation or duplicate ownership; cost if wrong: Task 6 must add any missing transactional switch behavior before G1.

Ruling: `persona_cursors.last_message_id` receives a restrictive foreign key to `conversation_messages.id`, because it is advanced from a durable conversation-message ID and conversation history is retained long term. Same-user ownership remains a Task 6 repository invariant rather than a Task 5 composite-key expansion; cost if wrong: the schema is more restrictive against orphan cursors but preserves the intended durable relationship.

- Task 5 — complete and independently approved after review round 1 plus scoped re-review. Commits: `342f257`, `09558ae3ef61acd019bf039df383d66661cd0fd4`. The repair added a named `(normalized_name, dob)` operational-profile identity constraint, a nullable restrictive persona-cursor message FK, direct `greenlet` runtime support with an AsyncSession canary, and history/reuse coverage for the relevant partial unique indexes. Coordinator fresh verification: 22 PostgreSQL schema tests, 113 total tests, Ruff check/format, strict mypy, lock check, and diff check passed. Both commits are reachable from `origin/main` at `09558ae3ef61acd019bf039df383d66661cd0fd4`.

Ruling: `SelectionTransition` and `CheckpointReturnTransition` carry their originating `source_selection_id`. Task 6 persistence requires this immutable branch identity to resolve the exact user/flow-version/service scope for leaf/checkpoint returns; a user-only fallback could mutate unrelated branches sharing a key. The repair additionally requires a user lock for all applies, including upsert-only transitions. Cost if wrong: the in-repo transition carrier gains a compatible field, but durable branch isolation and the Task 4 invariant that unrelated current selections remain untouched are preserved.

- Task 6 — complete and independently approved after review round 1 plus scoped re-review. Commits: `47c4dfe0487a2f434f2cf5f15e45d91899b67134`, `1163f3564e969c6b17d2a359f05494fc696b4b74`. The repair scoped transition persistence to a locked source branch, made rematching consider only active assignments, defined time-bounded safe delivery leasing, and reset UoW lock state per entry. Coordinator fresh verification: 35 focused live PostgreSQL tests, 130 total tests, Ruff check/format, strict mypy, lock check, and diff check passed. Both commits are reachable from `origin/main` at `1163f3564e969c6b17d2a359f05494fc696b4b74`.

Ruling: Compose's ignored local `env_file` is optional for read-only fresh `verify`, while `up` creates it with local credentials before Docker startup. This keeps the required `verify`-before-`up` sequence non-mutating and avoids source/default credentials. Cost if wrong: Compose requires v2.24+ for optional env-file support; the stated local runtime targets modern Docker Compose.

Ruling: Alembic downgrade retains `pgcrypto`. The initial revision cannot safely prove it created a pre-existing shared extension, so it drops F01 tables/types only; generated-database tests prove pre-existing `pgcrypto` survives downgrade/re-upgrade. Cost if wrong: a clean F01 downgrade leaves a harmless shared extension, rather than deleting another owner's dependency.

- Task 7 — code complete and independently approved after review round 1 plus scoped re-review. Commits: `4309e4c99af430b2599f8ef340be9a7c4b9ba2ce`, `e34e7ee26a9ac4f2e32990b3b73c7ee0ecafca00`. The repair makes fresh read-only Compose verification viable, preserves shared `pgcrypto`, and drives true Alembic upgrade/downgrade/re-upgrade parity tests in generated isolated databases. Coordinator fresh verification: 12 migration/guard tests passed with 1 host-only Compose skip; remaining 130 tests passed; explicit `alembic upgrade head`, `alembic check`, and `alembic downgrade base` passed; Ruff check/format, strict mypy, lock check, and diff check passed. Commits are reachable from `origin/main` at `e34e7ee26a9ac4f2e32990b3b73c7ee0ecafca00`. G1 remains operationally blocked only on executing Compose config/up/healthcheck on a host with a Compose CLI.

Ruling: Colima's installed guest Docker Compose v5.1.4 is accepted as the local Compose operational path. It executed the tracked config, exact `friendly-bot-u504` up/healthcheck/host-loopback migration and canary/down lifecycle against the same Colima Docker daemon; host `db_runtime_check.py verify` remains unexecuted because the host Docker client has no Compose plugin, but guard source/mismatch tests and guest real-Compose proof jointly satisfy the intended safety and runtime evidence. Cost if wrong: host CLI invocation itself remains unavailable, but no untracked substitute or external installation was used.

- Task 8 — complete and independently approved. Commits: `fa06b773c50d72e4a2fce5bea28a7f7df1cba8f6`, `2854afa56a8212dd5b88c6dd4dfbb9592fc38eed`, `c80690e513e95d7923d106c5115025bbfecc4aa1`. It adds acceptance examples for legal absent direct error handling and durable timestamp-claim idempotency, plus the F01 execution manifest. Coordinator fresh verification: new focused acceptance tests 2 passed; migration suite 12 passed with 1 documented host-only Compose skip; remaining F01 domain/persistence suite 129 passed; Alembic upgrade/check/downgrade, Ruff check/format, strict mypy, lock check, manifest JSON, and diff check passed. The manifest records passed-for-coordinator-review G1 Compose evidence and is reachable from `origin/main` at `c80690e513e95d7923d106c5115025bbfecc4aa1`.

- 2026-09-13 — F01 delivery-contract repair. Root-cause ruling: the prior `OutboundDeliveryRecord` omitted the typed durable message and lease ownership; `claim_next_safe` permanently excluded claimed rows; retry timing reused `created_at`; and `start_attempt` discarded correlation ID. This made T02's durable outbox impossible without forbidden ORM/session/persistence bypasses. The additive `0002_delivery_contract` migration now persists eligible-at, opaque unstarted claim token/expiry, confirmed Telegram message ID, and attempt correlation ID. `OutboundDeliveryRecord.message` is recursively immutable and provider-neutral; only a matching live claim token can renew or start; sending and uncertain rows are never recovered; exact retry-at, safe error, and confirmation persist. Fresh PostgreSQL red evidence was 3 failures for absent message/schedule/lease behavior, then green at 3 passed; the fresh `0001`-to-head reversible migration test was red for absent columns, then green at 1 passed. After reconciling upstream checkpoint-lineage work, local repair verification: complete F01 domain/persistence suite `145 passed, 1 skipped`; Alembic upgrade/check/downgrade, Ruff, mypy, and lock check exited 0. No product/architecture impact and no T02/R03/I04 worker-plan or status change: the repair stays within F01's pre-existing durable delivery boundary.

- 2026-09-13 — F01 delivery-contract review repair. Review found that the T02 dated design/plan still described the tokenless pre-repair F01 API, and F01 lacked an explicit recovery-then-stale-renewal assertion. Added `test_recovered_claim_rejects_stale_token_renewal`: under a temporary immediately reverted removal of `renew_claim` token validation it failed with `DID NOT RAISE DeliveryClaimLostError`; restored production code passed the test. Reconciled only the F01-generated contract references in T02's dated design/plan: commit claim; map immutable provider-neutral `claim.message` at T02; commit `start_attempt` with the live token before send; optionally renew only the live token; final safe finish with exact retry-at/result metadata; no ORM/session/migration shim and no sending/uncertain auto-replay. Review focused verification was 4 delivery tests plus 1 migration test green; complete F01 suite `146 passed, 1 skipped`. F01 implementation provenance is published commit `5b28c2e`; product and architecture masterplans require no change because this completes the approved durable-delivery implementation contract while preserving T02 ownership/scope.

- 2026-09-13 — Review-repair provenance: `13df44abd8045d640bed9779e843a61908e6749d` reconciled the dated T02 F01-delivery references and committed the stale-renewal evidence. Its pre-commit fetch found `HEAD` and `origin/main` at `5b28c2e` with no T02 external source; push/fetch/merge-base proved `13df44a` reachable from `origin/main`.
